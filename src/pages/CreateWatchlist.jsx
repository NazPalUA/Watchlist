import React from "react"
import './CreateWatchlist.scss'

export default function CreateWatchlist(props) {
    return (
        <div className={`create-watchlist ${props.className}`}>Create Watchlist Page</div>
    )
}